@page "/"

<PageTitle>Inicio - Gestión Usuarios</PageTitle>

<div class="container-fluid p-0">
    <!-- Hero Section -->
    <div class="hero-section text-center d-flex flex-column justify-content-center align-items-center animate-fade-in">
        <h1 class="display-3 fw-bold mb-3 text-primary">Gestión de Usuarios</h1>
        <p class="lead mb-5 text-muted" style="max-width: 600px;">
            Administra el acceso, roles y perfiles de usuarios de manera eficiente, segura y centralizada.
        </p>
        <div class="d-flex gap-3">
            <a href="users" class="btn btn-primary btn-lg shadow-lg">
                <i class="bi bi-people-fill me-2"></i> Gestionar Usuarios
            </a>
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank" class="btn btn-secondary btn-lg">
                <i class="bi bi-book me-2"></i> Documentación
            </a>
        </div>
    </div>

    <!-- Features Section -->
    <div class="container mt-5 pt-5">
        <div class="row g-4">
            <div class="col-md-4 animate-fade-in delay-100">
                <div class="card h-100 glass-panel p-4 text-center hover-card">
                    <div class="icon-wrapper mb-3 text-primary">
                        <i class="bi bi-shield-lock display-4"></i>
                    </div>
                    <h3 class="h4 fw-bold">Seguridad</h3>
                    <p class="text-muted">Control de acceso robusto y protección de datos sensibles para todos los usuarios.</p>
                </div>
            </div>
            <div class="col-md-4 animate-fade-in delay-200">
                <div class="card h-100 glass-panel p-4 text-center hover-card">
                    <div class="card-body">
                        <div class="icon-wrapper mb-3 text-secondary">
                            <i class="bi bi-person-gear display-4"></i>
                        </div>
                        <h3 class="h4 fw-bold">Roles</h3>
                        <p class="text-muted">Asignación flexible de roles y permisos para optimizar el flujo de trabajo.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 animate-fade-in delay-300">
                <div class="card h-100 glass-panel p-4 text-center hover-card">
                    <div class="card-body">
                        <div class="icon-wrapper mb-3 text-accent">
                            <i class="bi bi-graph-up display-4"></i>
                        </div>
                        <h3 class="h4 fw-bold">Eficiencia</h3>
                        <p class="text-muted">Interfaz optimizada para una gestión rápida y sin complicaciones.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .hero-section {
        min-height: 60vh;
        /* Background handled globally */
    }
    
    .hover-card {
        transition: transform 0.3s ease;
    }
    
    .hover-card:hover {
        transform: translateY(-10px);
        background: rgba(255, 255, 255, 0.05);
    }
</style>
